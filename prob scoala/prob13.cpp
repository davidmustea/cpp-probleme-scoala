#include<iostream>

using namespace std;

int main(){
    float a, b, c, x=0, y=0, z=0;
    
    cin >> a >> b >> c;

    if(a > 0)
        a = x;
    if(b > 0)
        b = y;
    if(c > 0)
        c = z;
    
    cout << x + y + z;
    
    return 0;
}